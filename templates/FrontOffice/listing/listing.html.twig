<!doctype html>
<html class="no-js" lang="fr">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="x-ua-compatible" content="ie=edge">
        <title>Liste des Trajets - TransportX</title>
        <meta name="description" content="Trouvez et réservez votre trajet parfait">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="manifest" href="{{ asset('FrontOffice/site.webmanifest') }}">
        <link rel="shortcut icon" type="image/x-icon" href="{{ asset('FrontOffice/assets/img/favicon.ico') }}">

        <!-- CSS here -->
        <link rel="stylesheet" href="{{ asset('FrontOffice/assets/css/bootstrap.min.css') }}">
        <link rel="stylesheet" href="{{ asset('FrontOffice/assets/css/owl.carousel.min.css') }}">
        <link rel="stylesheet" href="{{ asset('FrontOffice/assets/css/slicknav.css') }}">
        <link rel="stylesheet" href="{{ asset('FrontOffice/assets/css/flaticon.css') }}">
        <link rel="stylesheet" href="{{ asset('FrontOffice/assets/css/animate.min.css') }}">
        <link rel="stylesheet" href="{{ asset('FrontOffice/assets/css/magnific-popup.css') }}">
        <link rel="stylesheet" href="{{ asset('FrontOffice/assets/css/fontawesome-all.min.css') }}">
        <link rel="stylesheet" href="{{ asset('FrontOffice/assets/css/themify-icons.css') }}">
        <link rel="stylesheet" href="{{ asset('FrontOffice/assets/css/slick.css') }}">
        <link rel="stylesheet" href="{{ asset('FrontOffice/assets/css/nice-select.css') }}">
        <link rel="stylesheet" href="{{ asset('FrontOffice/assets/css/style.css') }}">
        <style>
            /* Styles spécifiques pour la page des trajets */
            .trip-card {
                border: none;
                border-radius: 10px;
                overflow: hidden;
                box-shadow: 0 10px 20px rgba(0,0,0,0.1);
                transition: all 0.3s ease;
                margin-bottom: 30px;
            }
            
            .trip-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 15px 30px rgba(0,0,0,0.2);
            }
            
            .trip-card .card-img-top {
                height: 200px;
                object-fit: cover;
                width: 100%;
            }
            
            .trip-card .card-body {
                padding: 20px;
            }
            
            .trip-card .card-title {
                font-size: 1.2rem;
                font-weight: 600;
                margin-bottom: 10px;
            }
            
            .trip-card .card-text {
                color: #666;
                font-size: 0.9rem;
            }
            
            .trip-card .card-footer {
                background: #f8f9fa;
                padding: 15px;
                border-top: none;
            }
            
            .trip-card .btn {
                border-radius: 50px;
                font-weight: 500;
                padding: 8px 20px;
                font-size: 0.85rem;
            }
            
            /* Style pour le champ de recherche */
            #searchTrip {
                padding: 15px 25px;
                border-radius: 50px;
                border: 1px solid #e5e5e5;
                width: 100%;
            }
            
            /* Style pour les filtres */
            .filter-card {
                background: #fff;
                border-radius: 10px;
                padding: 25px;
                box-shadow: 0 10px 20px rgba(0,0,0,0.05);
                margin-bottom: 30px;
            }
            
            .filter-card .form-control, 
            .filter-card .form-select {
                border-radius: 50px;
                padding: 10px 20px;
                margin-bottom: 15px;
            }
            
            /* Pagination */
            .pagination .page-link {
                border-radius: 50%;
                width: 40px;
                height: 40px;
                display: flex;
                align-items: center;
                justify-content: center;
                margin: 0 5px;
                border: none;
                color: #333;
            }
            
            .pagination .page-item.active .page-link {
                background: #4361ee;
                color: white;
            }
        </style>
    </head>

    <body>
        <!-- Preloader Start -->
        <div id="preloader-active">
            <div class="preloader d-flex align-items-center justify-content-center">
                <div class="preloader-inner position-relative">
                    <div class="preloader-circle"></div>
                    <div class="preloader-img pere-text">
                        <img src="{{ asset('FrontOffice/assets/img/logo/loder.jpg') }}" alt="">
                    </div>
                </div>
            </div>
        </div>
        <!-- Preloader End -->
        
        <!-- Header -->
        <header>
            <div class="header-area header-transparent">
                <div class="main-header">
                   <div class="header-bottom header-sticky">
                        <div class="container-fluid">
                            <div class="row align-items-center">
                                <!-- Logo -->
                                <div class="col-xl-2 col-lg-2 col-md-1">
                                    <div class="logo">
                                       <a href="{{ path('home') }}"><img src="{{ asset('FrontOffice/assets/img/logo/logo.png') }}" alt="TransportX"></a>
                                    </div>
                                </div>
                                <div class="col-xl-10 col-lg-10 col-md-8">
                                    <!-- Menu principal -->
                                    <div class="main-menu f-right d-none d-lg-block">
                                        <nav>
                                            <ul id="navigation">
                                                <li><a href="{{ path('home') }}">Accueil</a></li>
                                                <li><a href="{{ path('app_trips_index') }}">Trajets</a></li>
                                                <li><a href="{{ path('about') }}">À propos</a></li>
                                                <li><a href="{{ path('app_reservations_list') }}">Mes Réservations</a></li>
                                                <li><a href="{{ path('contact') }}">Contact</a></li>
                                                {% if is_granted('ROLE_USER') %}
                                                    <li class="add-list"><a href="{{ path('trips_new') }}"><i class="ti-plus"></i> Ajouter un trajet</a></li>
                                                    <li class="login"><a href="#"><i class="ti-user"></i> Profil</a></li>
                                                    <li class="register"><a href="{{ path('app_logout') }}"><i class="ti-power-off"></i> Déconnexion</a></li>
                                                {% else %}
                                                    <li class="login"><a href="{{ path('app_login') }}"><i class="ti-user"></i> Connexion</a></li>
                                                    <li class="register"><a href="{{ path('app_register') }}"><i class="ti-pencil"></i> Inscription</a></li>
                                                {% endif %}
                                            </ul>
                                        </nav>
                                    </div>
                                </div>
                                <!-- Menu Mobile -->
                                <div class="col-12">
                                    <div class="mobile_menu d-block d-lg-none"></div>
                                </div>
                            </div>
                        </div>
                   </div>
                </div>
           </div>
        </header>

        <main>
            <!-- Hero Start-->
            <div class="hero-area2 slider-height2 hero-overly2 d-flex align-items-center" style="background-image: url('{{ asset('FrontOffice/assets/img/hero/hero2.jpg') }}')">
                <div class="container">
                    <div class="row">
                        <div class="col-xl-12">
                            <div class="hero-cap text-center pt-50">
                                <h2>Recherchez votre trajet</h2>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- Hero End -->

            <!-- Categories Area Start -->
            <div class="categories-area section-padding30">
                <div class="container">
                    <!-- Section Title -->
                    <div class="row justify-content-center mb-4">
                        <div class="col-md-8 text-center">
                            <span class="text-muted">Trouvez le trajet parfait</span>
                            <h2>Nos trajets disponibles</h2>
                        </div>
                    </div>

                    <!-- Champ de recherche -->
                    <div class="row justify-content-center mb-5">
                        <div class="col-md-6">
                            <form method="get" action="{{ path('app_trips_index') }}" class="input-group">
                                <input type="text" id="searchTrip" class="form-control" placeholder="🔍 Exemple: Tunis - Ariana" name="q" value="{{ app.request.get('q') }}">
                                <button class="btn btn-primary" type="submit">Rechercher</button>
                            </form>
                        </div>
                    </div>

                    <!-- Flash messages -->
                    <div class="row justify-content-center">
                        <div class="col-md-8">
                            {% for label, messages in app.flashes %}
                                {% for message in messages %}
                                    <div class="alert alert-{{ label }} alert-dismissible fade show shadow-sm mt-4 text-center" role="alert">
                                        <strong>
                                            {% if label == 'success' %}
                                                ✅ Succès :
                                            {% elseif label == 'danger' %}
                                                ❌ Erreur :
                                            {% endif %}
                                        </strong>
                                        {{ message }}
                                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fermer"></button>
                                    </div>
                                {% endfor %}
                            {% endfor %}
                        </div>
                    </div>

                    <div class="row">
                        <!-- FILTRES -->
                        <div class="col-lg-4 mb-4">
                            <div class="filter-card">
                                <h4 class="mb-4"><i class="fas fa-filter me-2"></i>Filtres Avancés</h4>
                                <form method="get" action="{{ path('app_trips_index') }}">
                                    <div class="mb-3">
                                        <input type="text" name="departure" class="form-control" placeholder="Départ" value="{{ app.request.get('departure') }}">
                                    </div>
                                    <div class="mb-3">
                                        <input type="text" name="destination" class="form-control" placeholder="Destination" value="{{ app.request.get('destination') }}">
                                    </div>
                                    <div class="mb-3">
                                        <label for="priceRange">Prix maximum (<span id="priceLabel">{{ app.request.get('maxPrice')|default(500) }}</span> TND)</label>
                                        <input type="range" name="maxPrice" class="form-range" id="priceRange" min="0" max="1000" step="10" value="{{ app.request.get('maxPrice')|default(500) }}">
                                    </div>
                                    <div class="mb-3">
                                        <button type="submit" class="btn btn-primary w-100">Filtrer</button>
                                    </div>
                                </form>
                            </div>
                        </div>
                        
                        <!-- LISTE DES TRAJETS -->
                        <div class="col-lg-8">
                            <div class="row" id="tripsContainer">
                                {% for trip in trips %}
                                <div class="col-md-6 mb-4">
                                    <div class="card trip-card">
                                        {# Construction du nom de l'image selon la destination #}
                                        {% set imageName = trip.destination|lower|replace({' ': '-', "'": ''}) ~ '.png' %}
                                        <img src="{{ asset('FrontOffice/assets/img/' ~ imageName) }}" 
                                             class="card-img-top" 
                                             alt="{{ trip.destination }}"
                                             onerror="this.onerror=null;this.src='{{ asset('FrontOffice/assets/img/default.png') }}';">
                                        
                                        <div class="card-body">
                                            <h5 class="card-title">
                                                <a href="{{ path('app_trips_show', {'id': trip.id}) }}">
                                                    {{ trip.departure }} → {{ trip.destination }}
                                                </a>
                                            </h5>
                                            <p class="card-text">
                                                <i class="fas fa-calendar-alt me-2"></i> {{ trip.departureTime|date('d/m/Y H:i') }}<br>
                                                <i class="fas fa-bus me-2"></i> {{ trip.transportName }}<br>
                                                <i class="fas fa-road me-2"></i> {{ trip.distance }} km<br>
                                                <i class="fas fa-tag me-2"></i> <strong>{{ trip.price }} TND</strong>
                                            </p>
                                        </div>
                                        <div class="card-footer text-center">
                                            <a href="{{ path('app_trips_show', {'id': trip.id}) }}" class="btn btn-primary btn-sm">Détails</a>
                                            <a href="{{ path('app_trips_reserve', {'id': trip.id}) }}" class="btn btn-success btn-sm">Réserver</a>
                                            <a href="{{ path('app_weather_details', {'city': trip.destination}) }}" class="btn btn-info btn-sm">Météo</a>
                                        </div>
                                    </div>
                                </div>
                                {% else %}
                                <div class="col-12">
                                    <div class="alert alert-info text-center">
                                        Aucun trajet trouvé.
                                    </div>
                                </div>
                                {% endfor %}
                            </div>
                            
                            {# Pagination #}
                            {% if totalPages > 1 %}
                            <nav aria-label="Page navigation">
                                <ul class="pagination justify-content-center">
                                    {% for page in 1..totalPages %}
                                    <li class="page-item {% if page == currentPage %}active{% endif %}">
                                        <a class="page-link" href="{{ path('app_trips_index', app.request.query.all|merge({'page': page})) }}">{{ page }}</a>
                                    </li>
                                    {% endfor %}
                                </ul>
                            </nav>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
            <!-- Categories Area End -->

            <!-- Services Area Start -->
            <div class="services-area pt-150 pb-150 section-bg" data-background="{{ asset('FrontOffice/assets/img/gallery/section_bg02.jpg') }}">
                <div class="container">
                    <div class="row">
                        <div class="col-lg-12">
                            <!-- Section Tittle -->
                            <div class="section-tittle section-tittle2 text-center mb-80">
                                <span>Simple et efficace</span>
                                <h2>Comment ça marche</h2>
                            </div>
                        </div>
                    </div>
                    <div class="row justify-content-between">
                        <div class="col-lg-3 col-md-6">
                            <div class="single-services text-center mb-50">
                                <div class="services-icon">
                                    <span class="flaticon-search"></span>
                                </div>
                                <div class="services-cap">
                                    <h5><a href="#">1. Trouvez votre trajet</a></h5>
                                    <p>Recherchez parmi nos nombreux trajets disponibles</p>
                                </div>
                                <!-- Shpape -->
                                <img class="shape1 d-none d-lg-block" src="{{ asset('FrontOffice/assets/img/icon/serices1.png')}}">
                            </div>
                        </div>
                        <div class="col-lg-3 col-md-6">
                            <div class="single-services text-center mb-50">
                                <div class="services-icon">
                                    <span class="flaticon-calendar"></span>
                                </div>
                                <div class="services-cap">
                                    <h5><a href="#">2. Réservez & Payez</a></h5>
                                    <p>Effectuez votre réservation en quelques clics</p>
                                </div>
                                <img class="shape2 d-none d-lg-block" src="{{ asset('FrontOffice/assets/img/icon/serices2.png')}}">
                            </div>
                        </div>
                        <div class="col-lg-3 col-md-6">
                            <div class="single-services text-center mb-50">
                                <div class="services-icon">
                                    <span class="flaticon-airplane"></span>
                                </div>
                                <div class="services-cap">
                                    <h5><a href="#">3. Voyagez serein</a></h5>
                                    <p>Profitez d'un trajet confortable et sécurisé</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- Services Area End -->
        </main>

        <!-- Footer -->
        <footer>
            <div class="footer-area">
                <div class="container">
                   <div class="footer-top footer-padding">
                        <div class="row justify-content-between">
                            <div class="col-xl-3 col-lg-4 col-md-4 col-sm-6">
                                <div class="single-footer-caption mb-50">
                                    <div class="single-footer-caption mb-30">
                                        <div class="footer-logo">
                                          <a href="{{ path('home') }}"><img src="{{ asset('FrontOffice/assets/img/logo/logo2_footer.png') }}" alt=""></a>
                                        </div>
                                        <div class="footer-about">
                                            <p>TransportX - Votre partenaire de voyage fiable et sécurisé.</p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-xl-2 col-lg-2 col-md-4 col-sm-6">
                                <div class="single-footer-caption mb-50">
                                    <div class="footer-tittle">
                                        <h4>Liens rapides</h4>
                                        <ul>
                                            <li><a href="{{ path('home') }}">Accueil</a></li>
                                            <li><a href="{{ path('app_trips_index') }}">Trajets</a></li>
                                            <li><a href="{{ path('about') }}">À propos</a></li>
                                            <li><a href="{{ path('contact') }}">Contact</a></li>
                                        </ul>
                                    </div>
                                </div>
                            </div>
                            <div class="col-xl-2 col-lg-3 col-md-4 col-sm-6">
                                <div class="single-footer-caption mb-50">
                                    <div class="footer-tittle">
                                        <h4>Support</h4>
                                        <ul>
                                            <li><a href="#">FAQ</a></li>
                                            <li><a href="#">Conditions</a></li>
                                            <li><a href="#">Confidentialité</a></li>
                                        </ul>
                                    </div>
                                </div>
                            </div>
                            <div class="col-xl-3 col-lg-3 col-md-4 col-sm-6">
                                <div class="single-footer-caption mb-50">
                                    <div class="footer-tittle">
                                        <h4>Coordonnées</h4>
                                        <ul class="contact-info">
                                            <li><i class="ti-location-pin"></i> 123 Avenue des Voyages, Tunis</li>
                                            <li><i class="ti-email"></i> contact@transportx.com</li>
                                            <li><i class="ti-mobile"></i> +216 12 345 678</li>
                                        </ul>
                                    </div>
                                </div>
                            </div>
                        </div>
                   </div>
                    <div class="footer-bottom">
                        <div class="row d-flex justify-content-between align-items-center">
                            <div class="col-xl-9 col-lg-8">
                                <div class="footer-copy-right">
                                    <p>&copy; {{ "now"|date("Y") }} TransportX. Tous droits réservés.</p>
                                </div>
                            </div>
                            <div class="col-xl-3 col-lg-4">
                                <div class="footer-social f-right">
                                    <a href="#"><i class="fab fa-facebook-f"></i></a>
                                    <a href="#"><i class="fab fa-twitter"></i></a>
                                    <a href="#"><i class="fab fa-instagram"></i></a>
                                    <a href="#"><i class="fab fa-youtube"></i></a>
                                </div>
                            </div>
                        </div>
                   </div>
                </div>
            </div>
        </footer>

        <!-- Scroll Up -->
        <div id="back-top">
            <a title="Go to Top" href="#"> <i class="fas fa-level-up-alt"></i></a>
        </div>

        <!-- JS here -->
        <script src="{{ asset('FrontOffice/assets/js/vendor/modernizr-3.5.0.min.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/vendor/jquery-1.12.4.min.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/popper.min.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/bootstrap.min.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/jquery.slicknav.min.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/owl.carousel.min.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/slick.min.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/wow.min.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/animated.headline.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/jquery.magnific-popup.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/jquery.nice-select.min.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/jquery.sticky.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/contact.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/jquery.form.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/jquery.validate.min.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/mail-script.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/jquery.ajaxchimp.min.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/plugins.js') }}"></script>
        <script src="{{ asset('FrontOffice/assets/js/main.js') }}"></script>
        
        <!-- Script pour la recherche AJAX -->
        <script>
            $(document).ready(function() {
                // Mise à jour du label du prix
                $('#priceRange').on('input', function() {
                    $('#priceLabel').text($(this).val());
                });
                
                // Recherche AJAX optionnelle
                $('#searchTrip').on('keyup', function() {
                    let searchTerm = $(this).val();
                    if(searchTerm.length > 2) {
                        $.ajax({
                            url: "{{ path('app_trips_search') }}",
                            type: "GET",
                            data: { q: searchTerm },
                            success: function(data) {
                                $('#tripsContainer').html(data);
                            }
                        });
                    }
                });
            });
        </script>
    </body>
</html>