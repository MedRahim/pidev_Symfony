<header>
    <!-- Principal Header from index.html.twig (no 'Page' menu) -->
    <div class="header-area header-transparent">
        <div class="main-header">
            <div class="header-bottom header-sticky">
                <div class="container-fluid">
                    <div class="row align-items-center">
                        <!-- Logo -->
                        <div class="col-xl-2 col-lg-2 col-md-1">
                            <div class="logo">
                                <a href="{{ path('homePage') }}">
                                    <img src="{{ asset('FrontOffice/assets/css/Smart City building outline logo.png') }}" alt="Logo" style="max-height: 80px; max-width: 220px; background: #222; border: 2px solid #007bff; border-radius: 12px; padding: 8px; box-shadow: 0 4px 16px rgba(0,0,0,0.15); display: block; margin: 0 auto;">
                                </a>
                            </div>
                        </div>
                        <div class="col-xl-10 col-lg-10 col-md-8">
                            <!-- Main-menu -->
                            <div class="main-menu f-right d-none d-lg-block">
                                <nav>
                                    <ul id="navigation">
                                        <li><a href="{{ path('homePage') }}"><i class="fas fa-home"></i> Home</a></li>
                                        <li><a href="{{ path('market') }}"><i class="fas fa-shopping-cart"></i> Market</a></li>
                                        <li><a href="{{ path('hopital') }}"><i class="fas fa-hospital"></i> Hospital</a></li>
                                        <li><a href="{{ path('voyage') }}"><i class="fas fa-plane"></i> Voyage</a></li>
                                        <li><a href="{{ path('blog') }}"><i class="fas fa-newspaper"></i> Blog</a></li>
                                        <li><a href="{{ path('contact') }}"><i class="fas fa-envelope"></i> Contact</a></li>
                                        <li><a href="{{ path('app_user_edit', {'id': app.session.get('user_id')?:56}) }}"><i class="fas fa-user"></i> Profile</a></li>
                                        <li><a href="{{ path('app_logoutuser') }}"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
                                    </ul>
                                </nav>
                            </div>
                        </div>
                        <!-- Mobile Menu -->
                        <div class="col-12">
                            <div class="mobile_menu d-block d-lg-none"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</header>