<!DOCTYPE html>

<title>Edit Trips</title>

{% block body %}
    <h1>Edit Trips</h1>

    {{ include('trips/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_trips_index') }}">back to list</a>

    {{ include('trips/_delete_form.html.twig') }}
{% endblock %}
