{% extends 'base.html.twig' %}

{% block title %}PostLike{% endblock %}

{% block body %}
    <h1>PostLike</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post_like.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ post_like.createdAt ? post_like.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_post_like_index') }}">back to list</a>

    <a href="{{ path('app_post_like_edit', {'id': post_like.id}) }}">edit</a>

    {{ include('post_like/_delete_form.html.twig') }}
{% endblock %}
