<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Check Your Email | Your Market</title>
    <link rel="shortcut icon" type="image/x-icon" href="{{ asset('FrontOffice/assets/img/favicon.ico') }}">
    <link rel="stylesheet" href="{{ asset('FrontOffice/assets/css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('FrontOffice/assets/css/style.css') }}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background: #f5f7fa; min-height: 100vh; }
        .auth-wrapper {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #e0e7ff 0%, #f5f7fa 100%);
        }
        .auth-card {
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 8px 32px rgba(44, 62, 80, 0.08);
            padding: 2.5rem 2rem 2rem 2rem;
            max-width: 400px;
            width: 100%;
            position: relative;
        }
        .auth-card .brand {
            display: flex;
            justify-content: center;
            margin-bottom: 1.5rem;
        }
        .auth-card .brand img {
            width: 60px;
        }
        .auth-card h2 {
            font-weight: 700;
            color: #2A4B7C;
            text-align: center;
            margin-bottom: 0.5rem;
        }
        .auth-card p.subtitle {
            color: #6c757d;
            text-align: center;
            margin-bottom: 1.5rem;
        }
        .auth-card .icon {
            font-size: 3rem;
            color: #28A745;
            display: flex;
            justify-content: center;
            margin-bottom: 1.5rem;
        }
        .auth-card .btn-primary {
            background: linear-gradient(135deg, #2A4B7C 0%, #4e73df 100%);
            border: none;
            border-radius: 8px;
            font-weight: 600;
            font-size: 1.1rem;
            padding: 0.75rem 0;
            margin-top: 1.5rem;
        }
        .auth-card .btn-primary:hover {
            background: linear-gradient(135deg, #4e73df 0%, #2A4B7C 100%);
        }
    </style>
</head>
<body>
<div class="auth-wrapper">
    <div class="auth-card">
        <div class="brand">
            <img src="{{ asset('FrontOffice/assets/img/logo/logo.png') }}" alt="Logo">
        </div>
        <div class="icon">
            <i class="fas fa-envelope-circle-check"></i>
        </div>
        <h2>Check Your Email</h2>
        <p class="subtitle">We have sent you a verification email.<br>Please check your inbox and follow the instructions to verify your account.</p>
        <a href="{{ path('app_user_login') }}" class="btn btn-primary w-100 mt-3">Back to Login</a>
    </div>
</div>
<script src="{{ asset('FrontOffice/assets/js/bootstrap.bundle.min.js') }}"></script>
</body>
</html>