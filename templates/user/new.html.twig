<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register | Your Market</title>
    <link rel="shortcut icon" type="image/x-icon" href="{{ asset('FrontOffice/assets/img/favicon.ico') }}">
    <link rel="stylesheet" href="{{ asset('FrontOffice/assets/css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('FrontOffice/assets/css/style.css') }}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background: #f5f7fa; min-height: 100vh; }
        .auth-wrapper {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #e0e7ff 0%, #f5f7fa 100%);
        }
        .auth-card {
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 8px 32px rgba(44, 62, 80, 0.08);
            padding: 2.5rem 2rem 2rem 2rem;
            max-width: 400px;
            width: 100%;
            position: relative;
        }
        .auth-card .brand {
            display: flex;
            justify-content: center;
            margin-bottom: 1.5rem;
        }
        .auth-card .brand img {
            width: 60px;
        }
        .auth-card h2 {
            font-weight: 700;
            color: #28A745;
            text-align: center;
            margin-bottom: 0.5rem;
        }
        .auth-card p.subtitle {
            color: #6c757d;
            text-align: center;
            margin-bottom: 1.5rem;
        }
        .auth-card .form-control {
            border-radius: 8px;
            font-size: 1rem;
        }
        .auth-card .btn-success {
            background: linear-gradient(135deg, #28A745 0%, #43e97b 100%);
            border: none;
            border-radius: 8px;
            font-weight: 600;
            font-size: 1.1rem;
            padding: 0.75rem 0;
            margin-top: 0.5rem;
        }
        .auth-card .btn-success:hover {
            background: linear-gradient(135deg, #43e97b 0%, #28A745 100%);
        }
        .auth-card .auth-link {
            display: block;
            text-align: center;
            margin-top: 1.5rem;
            color: #2A4B7C;
            font-weight: 500;
            text-decoration: none;
            transition: color 0.2s;
        }
        .auth-card .auth-link:hover {
            color: #28A745;
            text-decoration: underline;
        }
        .auth-card .text-muted {
            font-size: 0.95rem;
        }
        .auth-card .alert {
            font-size: 0.95rem;
        }
    </style>
</head>
<body>
<div class="auth-wrapper">
    <div class="auth-card">
        <div class="brand">
            <img src="{{ asset('FrontOffice/assets/img/logo/logo.png') }}" alt="Logo">
        </div>
        <h2>Create Your Account</h2>
        <p class="subtitle">Register to start shopping and enjoy our services.</p>
        {% for message in app.flashes('error') %}
            <div class="alert alert-danger">{{ message }}</div>
        {% endfor %}
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
            <div class="mb-3">
                <label for="{{ form.profilePicture.vars.id }}" class="form-label">Profile Picture</label>
                {{ form_widget(form.profilePicture, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.profilePicture) }}
                <div class="form-text">JPEG, PNG or GIF (Max 2MB)</div>
            </div>
            <div class="mb-3">
                <label for="{{ form.CIN.vars.id }}" class="form-label">CIN/National ID</label>
                {{ form_widget(form.CIN, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.CIN) }}
            </div>
            <div class="mb-3">
                <label for="{{ form.Name.vars.id }}" class="form-label">Full Name</label>
                {{ form_widget(form.Name, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.Name) }}
            </div>
            <div class="mb-3">
                <label for="{{ form.Email.vars.id }}" class="form-label">Email</label>
                {{ form_widget(form.Email, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.Email) }}
            </div>
            <div class="mb-3">
                <label for="{{ form.Password.vars.id }}" class="form-label">Password</label>
                {{ form_widget(form.Password, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.Password) }}
            </div>
            <div class="mb-3">
                <label for="{{ form.Phone.vars.id }}" class="form-label">Phone</label>
                {{ form_widget(form.Phone, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.Phone) }}
            </div>
            <div class="mb-3">
                <label for="{{ form.birthday.vars.id }}" class="form-label">Date of Birth</label>
                {{ form_widget(form.birthday, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.birthday) }}
            </div>
            <div class="mb-3">
                <label for="{{ form.Address.vars.id }}" class="form-label">Address</label>
                {{ form_widget(form.Address, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.Address) }}
            </div>
            <div class="mb-3">
                <label for="{{ form.bio.vars.id }}" class="form-label">Biography</label>
                {{ form_widget(form.bio, {'attr': {'class': 'form-control', 'rows': 3}}) }}
                {{ form_errors(form.bio) }}
            </div>
            <button type="submit" class="btn btn-success w-100 mt-2">Register</button>
        {{ form_end(form) }}
        <a href="{{ path('app_user_login') }}" class="auth-link">Already have an account? <b>Sign In</b></a>
    </div>
</div>
<script src="{{ asset('FrontOffice/assets/js/bootstrap.bundle.min.js') }}"></script>
</body>
</html>